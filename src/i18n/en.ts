/**
 * @file English UI strings (UIStrings). Skeleton stub — full set ported in build phase B.
 */
import type { UIStrings } from "./index";

/** English translations. */
export const en: UIStrings = {
  "nav.home": "Home",
  "nav.archive": "Archive",
  "nav.about": "About"
};
