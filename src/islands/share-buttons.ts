/**
 * @file share-buttons island — social share actions. Hook ported in build phase C.
 */
import type { Spa } from "@moku-labs/web";

/** Share-buttons island: PORT mount hook that binds share/copy-link actions. */
export const shareButtons: Spa.ComponentDef = {
  name: "share-buttons",
  hooks: {}
};
