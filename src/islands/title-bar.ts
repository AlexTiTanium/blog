/**
 * @file title-bar island — terminal-style title bar effects. Hook ported in build phase C.
 */
import type { Spa } from "@moku-labs/web";

/** Title-bar island: PORT mount/nav hooks for the animated terminal title bar. */
export const titleBar: Spa.ComponentDef = {
  name: "title-bar",
  hooks: {}
};
